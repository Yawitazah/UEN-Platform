{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": "prisma migrate resolve --rolled-back 20260602000002_historical_redemptions --schema prisma/schema.postgres.prisma; prisma migrate deploy --schema prisma/schema.postgres.prisma",
    "startCommand": "node dist/server/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
